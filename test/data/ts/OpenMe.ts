namespace TestDataNamespace {
  /// This is a very bad name for the class. Instead, it should say something like "sdfoviu-isdfjk9_sdf"
  export interface OpenMe {
    ID: number;
    Type: ObjectType;
    Name: string;
  }
}